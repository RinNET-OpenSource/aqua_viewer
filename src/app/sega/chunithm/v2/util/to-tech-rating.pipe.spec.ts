import { ToTechRatingPipe } from './to-tech-rating.pipe';

describe('ToTechRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTechRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
