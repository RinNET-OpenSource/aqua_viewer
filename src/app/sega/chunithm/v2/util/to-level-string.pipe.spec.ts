import { ToLevelStringPipe } from './to-level-string.pipe';

describe('ToLevelStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ToLevelStringPipe();
    expect(pipe).toBeTruthy();
  });
});
