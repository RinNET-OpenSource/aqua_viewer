import { ToRaritySpritePipe } from './to-rarity-sprite.pipe';

describe('ToRaritySpritePipe', () => {
  it('create an instance', () => {
    const pipe = new ToRaritySpritePipe();
    expect(pipe).toBeTruthy();
  });
});
